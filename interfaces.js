// function myTodo(todo : {title: string, text: string}) {
//     console.log(todo.title+' : '+ todo.text)
// }
function myTodo(todo) {
    console.log(todo.title + ' : ' + todo.text);
}
var todo = { title: 'Work', text: 'to do that' };
myTodo(todo);
